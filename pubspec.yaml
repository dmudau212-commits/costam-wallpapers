name: costam_wallpapers
description: A custom wallpaper app for Unicorn.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.6
  url_launcher: ^6.0.9

flutter:
  uses-material-design: true
